apiVersion: v1
kind: Service
metadata:
  name: pyapp-service
  namespace: default           # ← Ajoute cette ligne
  labels:
    app: pyapp
spec:
  selector:
    app: pyapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
